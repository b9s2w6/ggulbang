<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
	"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
	
<mapper namespace="com.ssafy.house.model.mapper.BasketMapper">

<!-- 	<resultMap type="car" id="car">
		<result column="car_no" property="carNo"/>
		<result column="number" property="number"/>
		<result column="model" property="model"/>
		<result column="price" property="price"/>
		<result column="brand" property="brand"/>
	</resultMap> -->

<!-- 	<select id="selectBasketAll" resultMap="basket">

	</select>
	
	<select id="selectBasketDetail" parameterType="int" resultMap="basket">

	</select>
	
	<insert id="registBasket" parameterType="basket">

	</insert>
	
	<update id="updateBasket" parameterType="basket">

	</update>
	
	<delete id="deleteBasket" parameterType="int">


	</delete> -->


</mapper>