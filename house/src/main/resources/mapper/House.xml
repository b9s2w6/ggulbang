<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
	"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
	
<mapper namespace="com.ssafy.house.house.model.mapper.HouseMapper">
	<select id="selectHouseAll" resultType="house">
		select aptCode, buildYear, roadName, roadNameBonbun, roadNameBubun, roadNameSeq, roadNameBasementCode, roadNameCode, dong, bonbun, bubun, sigunguCode, eubmyundongCode, dongCode, landCode, apartmentName, jibun, lng, lat
		from houseinfo;
	</select>
	
	
	<select id="selectHouseDetail" resultType="house" parameterType="long">
		select aptCode, buildYear, roadName, roadNameBonbun, roadNameBubun, roadNameSeq, roadNameBasementCode, roadNameCode, dong, bonbun, bubun, sigunguCode, eubmyundongCode, dongCode, landCode, apartmentName, jibun, lng, lat
		from houseinfo
		where aptCode = #{aptCode};
	</select>
	
	<insert id="registHouse" parameterType="house">
		insert into houseinfo (aptCode, apartmentName)
		values(#{aptCode}, #{apartmentName})
	</insert>
	
	<delete id="deleteHouse" parameterType="long">
		delete from houseinfo
		where aptCode = #{aptCode};
	</delete>
	
</mapper>